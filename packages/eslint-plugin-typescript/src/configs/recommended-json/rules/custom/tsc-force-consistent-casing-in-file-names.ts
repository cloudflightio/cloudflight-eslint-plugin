import type { RuleDefinition } from '../../../rule-definition';

export const tscForceConsistentCasingInFileNames: RuleDefinition = {
    name: 'tsc-force-consistent-casing-in-file-names',
    options: ['error'],
};
