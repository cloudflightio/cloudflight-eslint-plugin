import type { RuleDefinition } from '../../../rule-definition';

export const tscExactOptionalPropertyTypes: RuleDefinition = {
    name: 'tsc-exact-optional-property-types',
    options: ['error'],
};
