import type { RuleDefinition } from '../../../rule-definition';

export const tscNoFallThroughCasesInSwitch: RuleDefinition = {
    name: 'tsc-no-fall-through-cases-in-switch',
    options: ['error'],
};
